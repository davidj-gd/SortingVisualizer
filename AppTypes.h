#pragma once

enum class Scene {
    Menu,
    Sorting,
};

enum class SortAlgorithm {
    Bubble,
    Insertion,
    Heap,
};

enum class BarVisual {
    Default,
    Compare,
    Swap,
    Sorted,
};
